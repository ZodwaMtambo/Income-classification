# Income-classification
a full PySpark ML pipeline on a real census dataset: cleaned and cast raw columns, encoded 8 categorical features with StringIndexer, and assembled features with VectorAssembler
